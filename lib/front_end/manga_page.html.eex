<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial;
      color: white;
      background-color: black;
    }

    /* Estilos para o modo escuro */
    body.light-mode {
      background-color: white;
      color: black;
    }

    h1 {
      font-size: 36pt;
      text-align: center;
    }

    /* Estilos para o título no modo escuro */
    body.light-mode h1 {
      color: black; /* Altera a cor do título para branco */
    }

    img {
      max-width: 700px;
      max-height: auto;
      display: block;
      margin: 0 auto; /* Centralizar a imagem */
    }
  </style>
  <title><%= title %></title>
</head>
<body id="body">
  <!-- Botão para alternar entre os modos claro e escuro -->
  <button onclick="toggleDarkMode()">Alternar Modo</button>

  <h1><%= title %></h1>
  <div class="image-container">
    <%= Enum.map(images, fn img -> %>
      <%= img %>
    <% end) %>
  </div>

  <script>
    function toggleDarkMode() {
      // Obtém o elemento body
      var body = document.getElementById("body");

      // Alterna entre as classes de estilo claro e escuro
      body.classList.toggle("light-mode");
    }
  </script>
</body>
</html>
